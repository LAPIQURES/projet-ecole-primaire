# Local database configuration for backend testing
# Copy this file to backend/.env.local and update values for your local MySQL.

PORT=5000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ecole2026
DB_USER=root
DB_PASSWORD=

# Optional: override JWT secret for local tests
JWT_SECRET=your-local-jwt-secret
